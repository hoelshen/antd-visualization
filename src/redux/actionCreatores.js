import axios from 'axios'

/* export const getTodoList = () =>{
    return ()=>{
      axios.ajax({
        url: '/pjt/project/list',
        data:{
          params:{
          }
        }
      }).then((res)=>{
            const action = getDataAction(res)
            dispatch(action)
        })
    }
}
 */